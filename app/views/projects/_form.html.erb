<%= form_for([@employee,@projects]) do |f| %>
  <% if @projects.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@projects.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @projects.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
